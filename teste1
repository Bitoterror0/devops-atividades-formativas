CREATE OR REPLACE PROCEDURE PRC_LOG_ACESSO (p_usuario IN VARCHAR2) IS
BEGIN
    INSERT INTO TB_LOG_SISTEMA (USUARIO, DATA_ACESSO, OPERACAO)
    VALUES (p_usuario, SYSDATE, 'LOGIN');
    COMMIT;
END; 
/
